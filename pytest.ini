[pytest]
markers =
    smoke: marker for smoke tests
    regression: marker for regression tests
    need_review: marker for test_project
    login_guest: marker for login_guest
    reg_user: marker for register_user